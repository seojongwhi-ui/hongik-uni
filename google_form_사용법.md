# 구글폼 / 구글시트 생성 방법

이 프로젝트의 설문은 구글폼으로 배포하고, 응답은 구글시트에 자동 저장하는 방식으로 구성했습니다.

## 방법

1. 브라우저에서 Google Drive를 엽니다.
2. 새로 만들기 > 더보기 > Google Apps Script를 선택합니다.
3. `create_google_form.gs` 파일 내용을 Apps Script 편집기에 붙여넣습니다.
4. 저장 버튼을 누르거나 `Ctrl + S`를 누릅니다.
5. 실행 버튼을 누르고 권한을 승인합니다.
6. 실행이 끝나면 Apps Script의 실행 로그에서 아래 링크를 확인합니다.
   - 응답자용 설문 링크
   - 편집용 설문 링크
   - 응답 구글시트 링크

## 실행 버튼이 비활성화될 때

아래 순서대로 확인하세요.

1. 로컬 IDE가 아니라 브라우저의 Google Apps Script 화면에서 실행해야 합니다.
2. Apps Script의 기본 파일 `Code.gs` 안에 `create_google_form.gs` 내용을 붙여넣습니다.
3. 저장 버튼을 누르거나 `Ctrl + S`를 누릅니다.
4. 상단 함수 선택 드롭다운이 어렵다면 그대로 둔 채 실행 버튼을 누릅니다.
5. `myFunction` 또는 `createDrivingEmotionBehaviorSurvey` 중 하나가 선택되어 있으면 둘 다 실행 가능합니다.

그래도 함수가 보이지 않으면 붙여넣은 코드 맨 위에 아래 함수명이 있는지 확인하세요.

```javascript
function myFunction() {
```

함수명이 보이는데도 실행 버튼이 계속 비활성화되면 브라우저 새로고침 후 다시 저장하고 함수 드롭다운을 확인하세요.

## 계정 액세스 경고가 뜰 때

아래 메시지가 뜨면 정상적인 권한 승인 단계입니다.

```text
프로젝트를 실행하려면 Google 계정 액세스가 필요합니다.
다시 시도한 후 이번에는 계정 액세스를 허용하세요.
```

해결 방법:

1. 실행 버튼을 다시 누릅니다.
2. Google 계정 선택 창이 뜨면 본인 계정을 선택합니다.
3. `이 앱은 Google에서 확인하지 않았습니다` 화면이 나오면 `고급`을 누릅니다.
4. `프로젝트 이름(으)로 이동`을 누릅니다.
5. 권한 목록을 확인한 뒤 `허용`을 누릅니다.
6. 실행이 끝난 뒤 왼쪽의 `실행` 또는 하단 실행 로그에서 설문 링크를 확인합니다.

이 권한은 스크립트가 본인 Google Drive에 구글폼과 응답용 구글시트를 만들기 위해 필요합니다.

만약 계정 선택 창이 안 뜨면 브라우저 팝업 차단을 해제하거나 Chrome에서 다시 시도하세요.

## 파일 설명

- `survey_questions.tsv`: 구글시트에 가져올 수 있는 설문 문항표입니다.
- `survey_response_template.tsv`: 응답 데이터 헤더만 정리한 템플릿입니다.
- `create_google_form.gs`: 구글폼과 응답 구글시트를 자동으로 생성하는 Apps Script입니다.

## 현재 설문 함수명

현재 구글폼 생성 함수명은 아래와 같습니다.

```javascript
function createDrivingEmotionBehaviorSurvey() {
```

상단 함수 선택이 어렵다면 `myFunction`을 그대로 실행해도 됩니다.
